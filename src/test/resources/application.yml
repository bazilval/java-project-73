base-url: "/api"

spring:
  application:
    name: my-task-manager

  jpa:
    show-sql: true

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml

  mvc:
    hiddenmethod:
      filter:
        enabled: true

logging:
  level:
    root: INFO